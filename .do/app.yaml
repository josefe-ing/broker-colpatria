name: broker-colpatria
region: nyc
static_sites:
  - name: broker-app
    source_dir: /
    index_document: index.html
    error_document: index.html
    environment_slug: html
    routes:
      - path: /
